import {Box} from '@chakra-ui/react'
import {FunctionComponent} from 'react'

const Header: FunctionComponent = () => {
  return <Box w='full' p={1} className='defaultColor'></Box>
}

export default Header
